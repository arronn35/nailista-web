rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Client reads/writes are blocked. All writes happen through Cloud Functions.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
